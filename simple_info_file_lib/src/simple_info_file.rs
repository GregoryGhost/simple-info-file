pub mod simple_info_file {
    pub fn count_line(data: &[&str]) -> usize {
        data.len()
    }

    fn count_ascii_symbols(data: &str) -> i32 {
        panic!("todo");
    }

    fn calc_size_data_in_ascii_bytes(data: &str) -> i32 {
        panic!("todo");
    }

    fn get_info_file(data: &str) -> i32 {
        panic!("todo");
    }
}